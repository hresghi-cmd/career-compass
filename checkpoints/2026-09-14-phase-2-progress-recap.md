# 2026-09-14 职业测试网站商业化复盘：阶段 2 等待验收

> 记录日期：2026-09-14  
> 当前目标：让同一条专属测试链接在刷新、关闭浏览器和更换设备后继续同一份未完成测试。  
> 当前阶段：阶段 0、阶段 1 已通过；阶段 2 已开发、测试并发布，等待用户验收；阶段 3 尚未开始。

## 一分钟恢复思路

项目位于 `E:\Chatgpt\搭建网站\career-test-site`，商业化阶段边界仍以 `COMMERCIALIZATION_ROADMAP.md` 为准。普通首页 `https://hresghi-cmd.github.io/career-compass/` 继续只展示产品说明，没有专属链接就不能开始测试。

阶段 2 已把答题答案、当前题号和进度版本保存到服务器数据库。浏览器本地只保存尚未成功上传的断网草稿；网络恢复后会自动补传。旧页面或另一台设备若持有过期版本，服务器会拒绝静默覆盖，并要求选择使用云端最新进度或保留本页进度。

当前阶段只实现“未完成测试的服务器进度”。最终确认、结果锁定、长期只读报告属于阶段 3，本次没有提前实现；商家后台和小红书自动发货也没有开始。

## 当前进度盘点

| 模块 | 状态 | 权威文件或说明 |
| --- | --- | --- |
| 商业化总路线 | 已确认 | `COMMERCIALIZATION_ROADMAP.md` |
| 普通公开首页 | 已确认 | 只能查看说明，不能免费开始测试 |
| 专属链接答题页面 | 已发布、待用户验收 | `app/access/access-route.tsx`、`app/page.tsx` |
| 云端进度接口 | 已发布并通过线上写入测试 | `worker/access-api.ts`、`worker/index.ts` |
| 数据库进度字段 | 已迁移到生产数据库 | `db/schema.ts`、`drizzle/0001_numerous_rhino.sql` |
| 断网草稿与自动补传 | 已实现、待用户体验 | `app/page.tsx` |
| 多设备旧版本保护 | 已通过自动化及线上冲突测试 | `progress_version` 条件更新 |
| 最终提交与报告锁定 | 未开始 | 阶段 3 范围 |
| 商家后台与链接库存 | 未开始 | 阶段 4 范围 |
| 小红书自动发货 | 未开始 | 阶段 7 范围 |

## 本次工作过程与关键决策

1. 启用 Sites 的 D1 数据库绑定，在原资格表上增加答案、进度版本和更新时间，不另建会重复记录资格的新表。
2. 专属链接页面从服务器读取权威进度；每次选择、返回修改和清空进度都会排队保存，避免连续操作相互覆盖。
3. 浏览器 `localStorage` 不再作为正常进度的唯一来源，只承担断网时保护刚选答案的作用。
4. 使用递增版本号阻止旧标签页覆盖新设备；冲突发生时交给使用者明确选择，服务器不会静默丢失新进度。
5. GitHub Pages 继续作为手机访问前端，云端接口部署在 Sites；已验证跨域许可、线上读写和冲突响应。
6. 演示链接在自动测试后已清回第 1 题，便于用户从空白状态开始验收。

## 重点产物索引

| 编号 | 文件 | 下次使用方式 |
| --- | --- | --- |
| 01 | `COMMERCIALIZATION_ROADMAP.md` | 阶段边界与验收制度的唯一权威来源 |
| 02 | `app/page.tsx` | 云端读取、保存提示、断网草稿、冲突处理和答题交互 |
| 03 | `app/access/access-route.tsx` | 将有效演示专属链接接入完整测试 |
| 04 | `worker/access-api.ts` | 服务器进度读取和写入接口 |
| 05 | `worker/index.ts` | 云端接口路由入口 |
| 06 | `db/schema.ts` | 资格和答题进度数据结构 |
| 07 | `drizzle/0001_numerous_rhino.sql` | 阶段 2 数据库升级迁移 |
| 08 | `tests/rendered-html.test.mjs` | 页面约束、数据库读写和旧版本冲突回归测试 |

## 状态边界

### 已确认

- 普通首页没有开始按钮，不能通过返回首页免费测试。
- 有效的未使用或进行中演示链接会进入完整测试，并从服务器读取同一份进度。
- 答题进度已存入生产数据库；换设备时不依赖原设备浏览器缓存。
- 断网时刚选答案先保存在当前设备，恢复网络后自动补传。
- 旧页面不能静默覆盖较新的云端进度。
- 没有新增姓名、手机号、身份证等个人资料字段。

### 当前软锚点或首选候选

- GitHub Pages 继续作为手机主要访问地址；Sites 目前只承担进度接口和备用网页发布。
- 保存成功提示尽量轻量，仅在断网、失败或冲突时显示较明显的说明框。

### 已淘汰或仅作边界对照

- 继续只使用浏览器本地缓存：不能跨设备，不符合阶段 2。
- 发生多设备冲突时“最后一次写入直接覆盖”：可能丢答案，已否决。
- 在阶段 2 顺便锁定最终报告或建立商家后台：属于范围漂移，未采用。

### 尚未确认

- 用户手机网络能否稳定请求 `chatgpt.site` 上的云端接口。此前手机曾无法直接打开该域名，因此必须以本轮手机验收结果为准。
- 保存状态文案在真实手机上是否足够清楚且不打扰答题。
- 阶段 2 是否通过，只能由用户完成电脑和手机验收后确认。

## 已知问题与工作规则

- 当前演示链接是共享测试凭证，不是销售库存；不要发给真实顾客或导入自动发货工具。
- 演示链接允许清除未完成进度并重新验收，这是阶段 2 的测试需要；“最终结果后不能重做”要到阶段 3 才实现。
- 如果手机显示“暂时无法读取测试进度”，优先记录手机网络、小红书内置浏览器或普通浏览器的差异，不要把阶段 2误判为通过。
- GitHub 命令行偶尔需要本机 `127.0.0.1:7890` 的一次性代理，不能写入永久 Git 配置。
- 阶段 2 未经用户回复“阶段 2 通过”之前，不进入阶段 3。

## 下次最推荐的工作顺序

1. **完成阶段 2 验收**：按交付说明先在电脑答到第 5 题，再用手机打开同一链接核对题号。
2. **验证断网补传和返回修改**：确认恢复网络后出现“已保存到云端”，刷新后修改仍存在。
3. **根据用户结论行动**：有问题只修阶段 2；用户明确回复“阶段 2 通过”后，下一次才讨论阶段 3。

## 下次开始时可直接使用的指令

> 读取 `checkpoints/2026-09-14-phase-2-progress-recap.md` 和 `COMMERCIALIZATION_ROADMAP.md`，继续职业测试网站商业化项目。阶段 0、阶段 1 已通过，阶段 2 已发布但要先核对我的手机与电脑验收反馈。普通首页必须保持不能免费开始测试；有问题只修阶段 2。除非我明确回复“阶段 2 通过”，不要进入最终报告锁定、商家后台或小红书自动发货。

## 本次收尾结论

阶段 2 已从“单机浏览器缓存”升级为带断网保护和多设备冲突防护的服务器进度，并完成自动化、生产数据库和线上接口验证；现在只等待用户在真实手机和电脑上验收。
